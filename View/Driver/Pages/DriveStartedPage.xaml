<Page x:Class="BookingApp.View.Driver.Pages.DriveStartedPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="300"
      Title="DriveStartedPage">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtPrice" Grid.Row="0" Text="{Binding Price}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Margin="0 50 0 0"/>

        <Button x:Name="btnEndDrive"
                Grid.Row="1"
                Content="Završi vožnju"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="20 10 10 20"
                Width="150" Height="50"
                Click="btnEndDrive_Click"/>
    </Grid>
</Page>