<Page x:Class="BookingApp.View.GuideView.Pages.TrackTourLivePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BookingApp.View.GuideView.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="430"
      Title="TrackTourLivePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox Grid.Row="0" ItemsSource="{Binding Tours}" DisplayMemberPath="Name" 
                  SelectedItem="{Binding SelectedTour}" Margin="10,0,0,0"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding CheckPoints}" Margin="10,0,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding PointText}" IsChecked="{Binding IsChecked}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
